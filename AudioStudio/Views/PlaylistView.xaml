<UserControl x:Class="AudioStudio.Views.PlaylistView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer x:Name="MainScroller"
                  HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto"
                  Background="#1E1E1E"
                  ScrollChanged="OnScrollChanged">
        <Grid x:Name="ContentGrid" ClipToBounds="True">
            <Canvas x:Name="GridLayer"/>
            <Canvas x:Name="TrackBgLayer"/>
            <Canvas x:Name="ClipLayer"
                    Background="#01000000"
                    MouseMove="ClipLayer_MouseMove"
                    MouseUp="ClipLayer_MouseUp"
                    MouseLeave="ClipLayer_MouseLeave"
                    MouseRightButtonDown="ClipLayer_MouseRightButtonDown"/>
            <Canvas x:Name="SelectionLayer" IsHitTestVisible="False"/>
            <Canvas x:Name="PlayheadLayer" Background="{x:Null}" IsHitTestVisible="False"/>
        </Grid>
    </ScrollViewer>
</UserControl>
